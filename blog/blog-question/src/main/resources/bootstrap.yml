spring:
  application:
    name: question-server # 当前服务的应用名
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # 注册中心地址
      config:
        server-addr: 127.0.0.1:8848 # 配置中心地址
        file-extension: yml # 配置中心的配置格式
  profiles:
    active: prod # 指定环境



